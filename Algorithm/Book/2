# a 주어질 자연수 갯수
# b 몇번 더할 것인지
# c 몇번까지 반복 할 수 있는지
#5 8 3 -> 5개의 숫자로 8번을 더할거고 같은 숫자는 3번씩 사용가능
#2 4 5 4 6 
#
a, b, c = map(int, input().split())
data = list(map(int, input().split()))

data.sort(reverse=True)
one = data[0] #제일 큰 수,
two = data[1] # 그담 큰수

answer = 0

while True:
    for i in range(c):
        if b == 0 : #반복할 횟수가 없으면 종료
            break
        answer += one #제일 큰 값을 출력값에 더해줌
        b-=1
    if b ==0:
        break
    answer += two #c만큼 돌고 그다음큰수 로 더해줌 
    b -= 1

print(answer)
